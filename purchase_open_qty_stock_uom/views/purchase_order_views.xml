<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <field name="qty_to_receive" position="after">
                <field
                    name="qty_to_receive_stock_uom"
                    attrs="{'column_invisible': [('parent.state', 'not in', ('purchase', 'done'))]}"
                    optional="hide"
                />
            </field>
        </field>
    </record>
    <record model="ir.ui.view" id="view_purchase_order_line_tree">
        <field name="model">purchase.order.line</field>
        <field
            name="inherit_id"
            ref="purchase_open_qty.view_purchase_order_line_tree"
        />
        <field name="arch" type="xml">
            <field name="qty_to_receive" position="after">
                <field name="qty_to_receive_stock_uom" optional="hide" />
            </field>
        </field>
    </record>
</odoo>
